import React from 'react';
import { Text, View } from "native-base"

const Temperature = () => {

    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>You place's average temperature</Text>
        </View>
    )
}

export default Temperature